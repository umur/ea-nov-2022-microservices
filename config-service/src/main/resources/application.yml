server:
  port: 7777
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kuylim/ea-config-repo.git
  application:
    name: config-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka